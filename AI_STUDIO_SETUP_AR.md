# تشغيل بورصة لبيب في Google AI Studio

مراجعة الإرشادات: 7 سبتمبر 2026. المسار المستخدم هو Google AI Studio في وضع Build.

## رأيي في الأمر المرفق

الأمر المرفق جيد في تقسيم المشروع وذكر البنود وFirebase والصلاحيات. النسخة المحسنة تجمع اختصاره مع تفاصيل التشغيل التي يحتاجها الموقع:

| النقطة | التحسين |
|---|---|
| التوقف بعد كل مرحلة | متابعة البناء تلقائيًا مع تحديث موجز؛ طلب الإعدادات الخارجية عندما تلزم فعلًا. |
| React أو Next.js | تثبيت اختيار React وTypeScript وVite مع خادم Node؛ يقلل تغييرات البنية. |
| الحسابات في الخادم أو قواعد Firestore | الخادم يتحقق ويحسب، وقواعد Firestore تمنع الوصول المباشر. |
| الحفظ والاعتماد | فصل المسودة عن المنشور، ومنع تكرار الرصيد، وحماية تعديلات المشرفين المتزامنة. |
| التوسع | إصدارات معايير، وسجل تعديل، وتصحيح تاريخي، وحالة نمو من صفر، وعضوية لقاء محفوظة. |
| الجوال | عروض اختبار محددة، وارتفاع مرن، وبطاقات عمودية وأسماء كاملة. |

## إعدادات أنصح بها

| الموضع | الاختيار |
|---|---|
| وضع العمل | Build لبناء التطبيق. |
| المنصة | Web app. |
| النموذج وخيارات التوليد | ابدأ بالافتراضي في Build؛ لا تحتاج قيم Temperature أو Top-P مخصصة لهذا المشروع. |
| الملفات المرجعية | الشعار الأصلي وصورة مرجعية واضحة، ثم الأمر الكامل. |
| التقنيات داخل الأمر | React + TypeScript + Node.js + Firebase. |
| إعداد الواجهة داخل الأمر | عربي RTL، خط Cairo، بطاقات للجوال وارتفاع تلقائي. |
| قاعدة البيانات والدخول | Firestore وGoogle Sign-in، مع حسابات مشرفين معتمدة. |
| Settings | قيم إعداد الخادم في Secrets، ومزامنة الكود من GitHub عند استخدامها. |
| المعاينة | راجع عرض 390 و430 بكسل، ثم شاشة واسعة؛ أرسل ملاحظة محددة لكل تعديل. |

نوع التقنية والألوان والمقاسات مواصفات تكتبها في الأمر؛ ليست كلها أزرارًا في شاشة إعدادات Studio. توثق Google بيئة React وNode، واستيراد GitHub، ومحرر Code، والتعديل بالمحادثة. [دليل Build الرسمي](https://ai.google.dev/gemini-api/docs/aistudio-build-mode)

## استخدام الحزمة الجاهزة

1. فك ضغط `Labeeb_AI_Studio_Starter.zip` على الحاسب.
2. أنشئ مستودعًا خاصًا في GitHub وارفع محتويات مجلد المشروع مع الحفاظ على المجلدات. يجب أن يكون `package.json` في جذر المستودع. يمكن رفع الملفات من واجهة GitHub أو GitHub Desktop. ملف `.env.example` مثال بلا أسرار؛ لا ترفع ملف `.env` الفعلي أو بيانات حساب خدمة.
3. افتح [Google AI Studio](https://aistudio.google.com/) ثم Build. من زر إضافة الملفات (+) اختر Import from GitHub واستورد المستودع.
4. الصق محتوى `docs/AI_STUDIO_PROMPT_AR.txt`، وأضف في أوله: «هذا مشروع قائم؛ حافظ على بنيته واختباراته، وشغّل معاينته أولًا ثم أكمل الربط». عند أي مشكلة توافق في تشغيل المعاينة، اطلب من Studio تكييف أمر التشغيل والمنفذ مع بيئته مع الإبقاء على الخادم وواجهات API.
5. اطلب تشغيل `npm run demo` لمراجعة الشكل بالأسماء الافتراضية، ثم انتقل إلى إعداد Firebase والتشغيل الحقيقي وفق الخطوات التالية.

مسار الاستيراد الموثق هو GitHub. لا تفترض أن إرفاق ZIP في المحادثة يعني أنه أصبح مشروعًا قابلًا للتشغيل. إذا أردت البداية دون GitHub، أنشئ مشروع Build جديدًا، وأرفق الشعار، والصق الأمر الكامل؛ سيبني Studio تطبيقًا وفقه، وقد تختلف الملفات الناتجة عن الحزمة المختبرة.

## إعداد Firebase والخادم

يدعم Studio إعداد Firestore وFirebase Authentication ودخول Google، ويوفر موضعًا لأسرار الخادم. اطلب منه ربط المشروع بهذه الخدمات ثم راجع القيم التالية. [دليل التطبيقات الكاملة الرسمي](https://ai.google.dev/gemini-api/docs/aistudio-fullstack)

| الاسم في الخادم | القيمة المطلوبة |
|---|---|
| `FIREBASE_WEB_CONFIG` | JSON لإعداد تطبيق Firebase Web من إعدادات مشروعك؛ يشمل apiKey وauthDomain وprojectId وappId. هذه إعدادات اتصال عامة وليست مفتاح حساب خدمة. |
| `OWNER_EMAIL` | البريد الفعلي لحساب Google الذي سيدير البرنامج أول مرة. |
| هوية الخادم | استخدم Application Default Credentials في Google Cloud مع الصلاحيات اللازمة لـFirestore والتحقق من مستخدمي Firebase Auth. |
| `FIREBASE_SERVICE_ACCOUNT` | بديل عند تعذر هوية التشغيل؛ JSON الحساب الخدمي في سر على الخادم فقط. لا تضعه في React أو مستودع GitHub. |
| `PORT` | اتبع منفذ بيئة التشغيل؛ الكود يدعمه. افتراضي التطوير 3000، وDocker يستخدم 8080. |

للتشغيل المحلي الفعلي: انسخ `.env.example` إلى `.env` واملأ القيم. يمكن استخدام `GOOGLE_APPLICATION_CREDENTIALS` بمسار ملف اعتماد خاص خارج المشروع بدل JSON في المتغير. لا تجمع خيارات الاعتماد بلا حاجة؛ يكفي مسار واحد صالح لهوية الخادم.

في Firebase:

1. فعّل Firestore وأنشئ تطبيق Web ضمن المشروع الصحيح.
2. فعّل Google ضمن Authentication، وأضف نطاق المعاينة المستقل والنطاق المنشور إلى Authorized domains عند الحاجة. إذا تعطلت النافذة المنبثقة داخل المعاينة، جرّب رابط الموقع في نافذة مستقلة.
3. طبّق محتوى `firestore.rules`: التطبيق يمر عبر خادم يتحقق من الهوية؛ المتصفح لا يحتاج إذن قراءة أو كتابة مباشرًا في Firestore.
4. اضبط صلاحيات هوية تشغيل الخادم لقراءة وكتابة بيانات Firestore والتحقق من مستخدمي Firebase Authentication. يختبر الخادم صحة رمز الدخول وحالة المستخدم؛ معرفة الرابط أو تسجيل الدخول من بريد عشوائي لا يمنح الإدارة. [التحقق من الرموز](https://firebase.google.com/docs/auth/admin/verify-id-tokens)
5. شغّل بالإعدادات الفعلية، ثم ادخل بحساب `OWNER_EMAIL`. أضف الشركات والمشرفين واللقاءات من الواجهة.

هذه الحزمة لا تحتاج مفتاح Gemini لكي تحسب الأسهم. دور الذكاء الاصطناعي هنا المساعدة في بناء البرنامج وتعديله.

## النشر والروابط

بعد نجاح الربط والاختبار، استخدم Publish في Studio وراجع خيار الاستضافة والتكلفة الظاهر لحسابك. توجد خيارات نشر تختلف أهليتها ومتطلباتها؛ لا تفترض مجانية الاستضافة وقاعدة البيانات لكل حساب. [دليل النشر الرسمي](https://ai.google.dev/gemini-api/docs/aistudio-deploying)

استخدم عنوان التطبيق المنشور للعامة، ويكون رابط المشرفين هو العنوان نفسه مع `/admin`. انشر رابط التطبيق للطلاب والأهالي بعد اعتماد أسماء العرض. مشاركة مشروع Studio نفسه تعطي وصولًا للمشروع وفق إعدادات مشاركته، وهي غرض مختلف عن رابط موقع النتائج.

قبل مشاركة الرابط تحقق من حساب مدير فعلي وحساب مشرف فئة واحدة وحساب غير معتمد. جرّب من جهازين أن المسودة لا تظهر وأن الاعتماد يحدث النتائج وأن أسماء العرض فقط هي المكشوفة في العرض العام. هذه الاختبارات تحتاج إعداداتك الفعلية ولم تُنفّذ على حساباتك في الحزمة المقدمة.

## أوامر قصيرة للتطوير بعد الاستيراد

«أضف ميزة [وصف الميزة] داخل البنية الحالية. راجع أثرها على الرصيد والصلاحيات واللقاءات السابقة. حافظ على الشعار وتجربة الجوال، وأضف اختبارًا للسلوك المؤثر ثم شغّل الاختبارات والبناء. وضح الملفات التي تغيرت.»

«راجع هذه الشاشة على عرض 390 بكسل: حسّن المسافات وحجم النص ووضوح الأزرار، وحافظ على البيانات والحسابات. لا تغير ألوان الهوية أو نظام التنقل إلا إذا تطلبت المشكلة ذلك.»

«جهز ربط Firebase الفعلي باستخدام إعدادات المشروع، واجعل اعتماد النتائج على الخادم مع Google Sign-in وحسابات معتمدة. إن احتجت قيمة لا تملكها، حدد اسمها وموضع إدخالها، ثم أكمل بقية الأجزاء المتاحة.»
